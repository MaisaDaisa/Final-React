import { Builder, Home } from '../pages';

export const routes = {
    HOME: {
        url: '/',
        component: Home,
    },
    BUILDER: {
        url: '/builder',
        component: Builder,
    },
};
